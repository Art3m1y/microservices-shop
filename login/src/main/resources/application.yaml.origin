spring:
  datasource:
    driver-class-name:
    url:
    username:
    password:

  jpa:
    properties:
      hibernate:
        show_sql:
        dialect:

  mail:
    host:
    port:
    username:
    password:
    properties.mail.smtp:
      auth:
      starttls.enable:

  application:
    name:

secret.key:
  for.access:
  for.refresh:

server:
  port:

eureka:
  client:
    service-url:
      defaultUrl:

